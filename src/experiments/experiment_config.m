function config = experiment_config(dataset)
    % EXPERIMENT_CONFIG Editable settings; explicit run_experiment starts the sweep.
    if nargin < 1
        dataset = 'COIL';
    end
    config = struct('dataset', char(dataset), 'preset_family', 'tt', ...
                    'methods', {{'TTDA', 'TWTTDA', 'ThW'}}, 'data_dir', [], ...
                    'seed', 0, 'repeats', 10, 'train_per_class', [], 'tensor_shape', [], ...
                    'tau', [], 'tau_by_method', struct(), 'lambda', [], ...
                    'validation_per_class', 5, 'validation', struct(), 'ablations', [], ...
                    'parameters', struct(), 'ranks', [], 'noise', 0, 'scale', 255, ...
                    'save_results', true, 'output_dir', [], 'plot', false, 'figure_visible', 'on', 'images', [], 'labels', []);
end
